// Template for src/environment/environment.ts (git-ignored).
// Copy this file to environment.ts and fill in the real values
// (both keys are public by nature; CI generates the file from secrets).
export const environment = {
    firebaseApiKey: '<firebase-api-key>',
    reCaptchaKey: '<recaptcha-v3-site-key>'
};
